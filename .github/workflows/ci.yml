name: CI

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        stage: [prove6, behave]
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - uses: Raku/setup-raku@v1
        with:
          raku-version: 'latest'
      - name: Raku version
        run: raku --version
      - name: Install Dependencies
        run: zef install --/test --test-depends --deps-only .
      - name: Install App::Prove6
        run: zef install --/test App::Prove6
      - name: Install DBIish
        run: zef install --/test DBIish
      - name: Run ${{ matrix.stage }}
        if: matrix.stage != 'behave'
        run: ./test.raku ${{ matrix.stage }}
      - name: Run behave under coverage
        if: matrix.stage == 'behave'
        run: |
          ./test.raku behave --coverage --coverage-branch \
            --coverage-format html --coverage-output coverage
      - name: Keep the report for the coverage job
        if: matrix.stage == 'behave'
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report
          path: coverage

  coverage:
    needs: test
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deploy.outputs.page_url }}
    steps:
      - name: Take the report from the behave run
        uses: actions/download-artifact@v4
        with:
          name: coverage-report
          path: coverage
      - name: Name the site
        run: echo coverage.behave.dev > coverage/CNAME
      - name: Upload the report
        uses: actions/upload-pages-artifact@v3
        with:
          path: coverage
      - name: Publish to coverage.behave.dev
        id: deploy
        uses: actions/deploy-pages@v4
